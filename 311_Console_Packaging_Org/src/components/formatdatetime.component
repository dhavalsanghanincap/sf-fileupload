<apex:component access="global" controller="Console311_FormateDatetimeController">
  <apex:attribute assignTo="{!dateTimeValue}" description="The DateTime value to be rendered based upon the user's
     locale" name="date_Timevalue" type="DateTime"></apex:attribute>
  <apex:attribute assignTo="{!formatStr}" description="Date format String." name="format" type="String"></apex:attribute>
  <apex:attribute assignTo="{!timezoneStr}" description="Date timezone String." name="timezone" type="String"></apex:attribute>

   {!TimeZoneValue} 
</apex:component>